# 软件安装检查

公共只读模块，检查三项最低开发条件：开发工具、Python 基础开发环境、编辑器与 Python 集成。支持明确验证 VS Code、Visual Studio、Cursor 和 PyCharm；未列入范围的编辑器不会被冒充为已验证通过。

该模块与“Codex 环境补全”共用一个助手程序和一个窗口实例，但不会安装大型软件、编辑器扩展或 Visual Studio 工作负载。
